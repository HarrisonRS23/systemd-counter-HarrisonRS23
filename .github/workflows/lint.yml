name: counter-build-test
run-name: ${{ github.actor }} is linting the deb file 🚀
on: [push]
jobs:
  build:
    timeout-minutes: 2
    runs-on: ubuntu-latest
    steps:
      - name: Run lint deb
        run: |
          make lint-deb
      - run: echo "🍏 This job's status is ${{ job.status }}."
